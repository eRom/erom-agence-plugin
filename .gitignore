# macOS
.DS_Store

# Variante générée non encore distribuée (Codex : en attente d'aval).
# gemini-agence-plugin/ EST versionné (install directe depuis le repo).
/codex-agence-plugin/

# Outillage / build (futur transpileur TS)
node_modules/
dist/

# Config Claude locale (la settings.json partagée reste versionnée)
**/.claude/settings.local.json

# Conception interne + outillage de mainteneur (non distribué au public)
/docs/
/.claude/commands/
